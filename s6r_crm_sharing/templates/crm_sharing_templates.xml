<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="crm_sharing_portal" name="CRM Sharing View in Portal">
        <t t-call="portal.frontend_layout">
            <!-- To add the class on div#wrapwrap to remove the overflow -->
            <t t-set="pageName" t-value="'o_crm_sharing_container'"/>
            <t t-set="no_footer" t-value="true"/>
            <t t-call="s6r_crm_sharing.crm_sharing"/>
        </t>
    </template>

    <template id="crm_sharing" name="CRM Sharing View">
        <!--    We need to forward the request lang to ensure that the lang set on the portal match the lang delivered -->
        <iframe class="w-100 h-100" frameborder="0" t-attf-src="/{{ request.context.get('lang', 'en_US') }}/my/opportunities/crm_sharing"/>
    </template>

    <template id="crm_sharing_embed" name="CRM Sharing Embed">
        <t t-call="web.layout">
            <t t-set="head_crm_sharing">
                <script type="text/javascript">
                    odoo.__session_info__ = <t t-out="json.dumps(session_info)"/>;
                    odoo.loadMenusPromise = Promise.resolve();
                </script>
                <base target="_parent"/>
                <t t-call-assets="web.assets_common" t-js="false"/>
                <t t-call-assets="project.webclient" t-js="false"/>
                <t t-call-assets="web.assets_common" t-css="false"/>
                <t t-call-assets="project.webclient" t-css="false"/>
                <t t-call="web.conditional_assets_tests"/>
            </t>
            <t t-set="head" t-value="head_crm_sharing + (head or '')"/>
            <t t-set="body_classname" t-value="'o_web_client o_crm_sharing'"/>
        </t>
    </template>
</odoo>
